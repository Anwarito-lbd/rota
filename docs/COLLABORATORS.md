# Collaborators

Pending invite requested for: zinedinechaiblaine@gmail.com (Write access)

GitHub personal repos require a GitHub username to invite via API.
Once they create/share their GitHub username, run:

```bash
gh api -X PUT repos/Anwarito-lbd/rota/collaborators/USERNAME -f permission=push
```

Or accept invite from the GitHub UI after email invitation from Settings → Collaborators.
